---

- name: install full environment
  include: depend.yml
  when: genesis_isolated

- name: create
  include: create.yml
  when: not genesis_destroy
  
- name: destroy
  include: destroy.yml
  when: genesis_destroy

# TODO CREATE .gitignore or append to gitignore for sensitive data in *.tfvars and create, destroy
